// use super::board_utils::{Board, Direction, Move};
//
// pub fn get_next_move(board: &mut Board) -> Move {
//     let depth = 2;
//     Move::new(Some(Direction::UP), 0.0)
// }
//
// fn process_children(board: &mut Board, depth: u32, direction: &Direction) -> Move {
//     let actions = board.move_tiles(direction);
//     board.get_all_generate_actions();
//     Move::new(Some(Direction::UP), 0.0)
// }
